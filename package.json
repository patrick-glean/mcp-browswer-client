{
  "name": "mcp-browser-client",
  "version": "0.1.0",
  "description": "MCP Browser Client with WASM service worker",
  "scripts": {
    "start": "http-server public",
    "build": "./wasm-build.sh",
    "setup:python": "python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt",
    "start:python": "source venv/bin/activate && python3 test_mcp_server.py",
    "dev": "concurrently \"npm run start\" \"npm run start:python\""
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "concurrently": "^8.2.2"
  }
} 